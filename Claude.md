# HamsterManager 项目文档

## 项目概述

HamsterManager 是一个功能强大的数据库管理工具，支持连接到 MySQL 数据库并进行可视化管理。

## 主要功能

### 数据库连接管理
- 支持保存和管理多个数据库连接配置
- 支持连接到 MySQL 数据库
- 支持创建、编辑和删除连接

### 数据库操作
- 查看数据库列表
- 查看数据库状态和表信息
- 创建新数据库
- 编辑数据库属性
- 导出数据库（结构或结构+数据）
- 执行 SQL 文件导入

### 表操作
- 查看表列表
- 查看表结构和数据
- 创建新表
- 编辑表结构
- 重命名表
- 截断表
- 删除表
- 导出表 SQL 文件

### 数据操作
- 查看表数据
- 添加新数据
- 编辑现有数据
- 删除数据
- 支持数据过滤和搜索
- 支持数据分页

### 查询功能
- 内置 SQL 查询编辑器
- 支持执行 SQL 查询
- 支持查看查询结果

## 使用说明

### 连接到数据库
1. 点击左侧边栏的 "New Connection" 按钮
2. 填写数据库连接信息
3. 点击 "Connect" 按钮连接到数据库

### 管理数据库
1. 右键点击连接名称，选择 "Create Database" 创建新数据库
2. 点击数据库名称查看数据库状态
3. 右键点击数据库名称，可进行导出、编辑等操作

### 管理表
1. 点击数据库名称展开表列表
2. 双击表名称查看表数据
3. 右键点击表名称，可进行打开、编辑、重命名、截断、删除等操作

### 编辑表结构
1. 右键点击表名称，选择 "Edit Table"
2. 在表设计器中可以添加、删除和修改列
3. 可以添加、删除和修改索引
4. 可以设置表属性
5. 点击 "Save" 按钮保存更改

## 技术栈

- React 18
- TypeScript
- Tailwind CSS
- Electron
- MySQL 2

## 项目结构

```
src/
├── components/          # React 组件
├── store/              # 状态管理
├── types/              # 类型定义
├── lib/                # 工具函数
├── App.tsx            # 主应用组件
└── main.tsx           # 应用入口
```

## 开发说明

### 安装依赖
```bash
npm install
```

### 启动开发服务器
```bash
npm run dev
```

### 构建应用
```bash
npm run build
```

## 许可证

MIT